@props(['ruta', 'nombre'])

<div class="text-center">
    <img src="{{$ruta}}" class="mx-auto mb-4 w-32 rounded-full" alt="Avatar" />
    <p class="mb-2 text-xl font-medium leading-tight">{{$nombre}}</h5>
</div>